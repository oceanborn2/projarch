tounix.pl : A script capable of converting a set of files from dos/win line endings to unix line endings. Files are only modified if at meast one line of the file is in the wrong format (dos/win) and if the file is considered writable. Permissions are preserved.

